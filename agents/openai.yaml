interface:
  display_name: "Encuadre Production API"
  short_description: "Use the Encuadre production portal API safely"
  default_prompt: "Use $encuadre-production-api to operate the Encuadre production portal API safely."
